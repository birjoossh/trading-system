# Core
pandas>=1.3.0
numpy>=1.20.0
pyyaml>=6.0
tables>=3.8.0        # HDF5 backtest data (JioH5Adapter)

# API gateway
fastapi>=0.110.0
uvicorn>=0.22.0

# Brokers (optional)
# ibapi>=9.81.1      # Interactive Brokers — install manually if you use the IB broker;
#                    # the core and paper broker work without it.

# Development
pytest>=6.0.0
ruff
